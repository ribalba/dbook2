OPTION(BUILD_SHARED_LIBS "turn OFF for .a libs" ON)
ADD_LIBRARY(libdbook libdbook)
SET_TARGET_PROPERTIES(libdbook PROPERTIES LINKER_LANGUAGE C)

SET_TARGET_PROPERTIES(libdbook 
PROPERTIES
	SOVERSION 0.1
	VERSION 0.1
)


